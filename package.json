{
  "name": "@volta-oss/chatbot-intake-blueprint",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "A safe blueprint for building small-business chatbots that answer FAQs without pretending to be human experts.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "npm run typecheck",
    "format:check": "node scripts/check-format.js",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm run format:check && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/chatbot-intake-blueprint:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "ai-automation",
    "digital-equity",
    "local-first",
    "oss",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/chatbot-intake-blueprint.git"
  },
  "homepage": "https://volta-npo.github.io/chatbot-intake-blueprint/",
  "bugs": {
    "url": "https://github.com/volta-npo/chatbot-intake-blueprint/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.1"
  }
}
